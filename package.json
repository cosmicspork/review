{
  "name": "review",
  "version": "0.1.0",
  "type": "module",
  "private": false,
  "license": "MIT",
  "description": "Local pre-publish review queue: agents submit diffs/MRs/PRs/tickets; a human approves, edits, and comments before publishing.",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --hot run server.ts",
    "cleanup": "bun run cleanup.ts",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "adf-to-md": "^1.2.1",
    "chokidar": "^5.0.0",
    "diff2html": "^3.4.56",
    "lit": "^3.3.3",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
